<div class="graphics">
  <%= render "layouts/drawgraph"%>
</div><!-- .div class graphics-->
  <h3>Currencies for <%= current_user.email %></h3>
  <%= form_for @countries_user,remote:true do |f| %>
    <%=f.hidden_field :target, :value=>'currencies'%>
  <%=f.submit "Mark all checked as collected"%>
  <table class="order-table table">
    <thead>
    <tr>
      <th>Name <input type="search" class="light-table-filter" data-table="order-table" placeholder="Filter"></th>
      <th>Code</th>
      <th>Status</th>
      <th>details</th>
      <th><input type="checkbox" class="check_all"></th>
    </tr>
    </thead>
    <%= render @currencies %>
  <%end%>
  </table>